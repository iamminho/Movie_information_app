import { Link } from "react-router-dom";

const Detail = () => {
  return (
    <div>
      <button>
        <Link to="/">Go Home</Link>
      </button>
      <h1>Detail</h1>
    </div>
  );
};

export default Detail;
